const arr = [[1,2,3,4,5]]
// this is nested array 

// use Destructuring to get the the first element inside nested arr 
// replace ? with your solution and print it 
// you can't use index or store result in another variable (just replace ? ) 
let [[first]] = arr
console.log(`${first}`)